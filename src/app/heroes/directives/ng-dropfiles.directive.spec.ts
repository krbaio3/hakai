import { NgDropfilesDirective } from './ng-dropfiles.directive';

describe('NgDropfilesDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDropfilesDirective();
    expect(directive).toBeTruthy();
  });
});
