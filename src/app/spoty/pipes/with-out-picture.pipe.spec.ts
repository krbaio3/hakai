import { WithOutPicturePipe } from './with-out-picture.pipe';

describe('WithOutPicturePipe', () => {
  it('create an instance', () => {
    const pipe = new WithOutPicturePipe();
    expect(pipe).toBeTruthy();
  });
});
